{
  "$defs": {
    "ConnectedRepo": {
      "description": "A corpospec repository that this tenant has connected for\ncross-repo PathRef resolution. External refs use `<alias>:<path>`\nsyntax (e.g. `ug:entity/company`).",
      "properties": {
        "alias": {
          "type": "string"
        },
        "branch": {
          "default": "main",
          "type": "string"
        },
        "corpospec_dir": {
          "default": ".corpospec",
          "type": "string"
        },
        "last_sync": {
          "anyOf": [
            {
              "$ref": "#/$defs/IsoDate"
            },
            {
              "type": "null"
            }
          ]
        },
        "url": {
          "type": "string"
        }
      },
      "required": [
        "alias",
        "url"
      ],
      "type": "object"
    },
    "IsoDate": {
      "description": "ISO 8601 date (YYYY-MM-DD).",
      "format": "date",
      "type": "string"
    },
    "Lei": {
      "description": "ISO 17442 Legal Entity Identifier (20-character alphanumeric).",
      "pattern": "^[A-Z0-9]{18}[0-9]{2}$",
      "type": "string"
    },
    "Pillar": {
      "description": "Pillar name.",
      "enum": [
        "entity",
        "financials",
        "metrics",
        "product",
        "market",
        "people",
        "legal",
        "governance",
        "integrations",
        "brand"
      ],
      "type": "string"
    },
    "Semver": {
      "description": "Semantic version string (MAJOR.MINOR.PATCH).",
      "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)$",
      "type": "string"
    }
  },
  "$id": "https://corpospec.com/schemas/v0.9.2/manifest.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Repository metadata declaring spec version and company identity.",
  "properties": {
    "company_id": {
      "description": "Stable, machine-readable company identifier. Lowercase,\nkebab- or snake-case, matches the repository's canonical slug\n(e.g. `unstarter`, `acme-corp`). Free-form string, not a PathRef.",
      "pattern": "^[a-z0-9]+(?:[_-][a-z0-9]+)*$",
      "type": "string"
    },
    "connected_repos": {
      "items": {
        "$ref": "#/$defs/ConnectedRepo"
      },
      "type": "array"
    },
    "corpospec_version": {
      "$ref": "#/$defs/Semver",
      "description": "CorpoSpec specification version this repository conforms to\n(SemVer). Determines which schema set validates the files."
    },
    "created": {
      "$ref": "#/$defs/IsoDate",
      "description": "Date the repository was initialised for this company."
    },
    "lei": {
      "anyOf": [
        {
          "$ref": "#/$defs/Lei"
        },
        {
          "type": "null"
        }
      ],
      "description": "Optional ISO 17442 Legal Entity Identifier assigned by an LOU.\nRequired only for entities subject to regulatory LEI obligations."
    },
    "pillars": {
      "description": "Pillars the repository currently maintains. Subset of the canonical\nten-pillar CorpoSpec set; absent pillars mean the company has not yet\nmodelled that domain.",
      "items": {
        "$ref": "#/$defs/Pillar"
      },
      "type": "array"
    },
    "schema_version": {
      "$ref": "#/$defs/Semver",
      "description": "Independent version counter for this repository's content shape.\nIncrement when consumers must re-read the full repository."
    }
  },
  "required": [
    "corpospec_version",
    "company_id",
    "created",
    "schema_version",
    "pillars"
  ],
  "title": "Manifest",
  "type": "object"
}